// Basic arithmetic and variables
var x = 10;
var y = 20;
var sum = x + y;
print(sum);

// Conditional
if (x < y) {
    print("x is less than y");
} else {
    print("x is greater than or equal to y");
}

// Loop
var i = 0;
while (i < 5) {
    print(i);
    i = i + 1;
}
